# Run health check on pre-commit
echo "🔍 Running pre-commit checks..."

# Type check all packages
echo "📝 Type checking..."
bun run type-check
if [ $? -ne 0 ]; then
  echo "❌ Type check failed"
  exit 1
fi

# Lint and format check
echo "✨ Linting..."
bun run lint
if [ $? -ne 0 ]; then
  echo "❌ Linting failed"
  echo "💡 Run 'bun run lint:fix' to fix automatically"
  exit 1
fi

echo "✅ All checks passed!"